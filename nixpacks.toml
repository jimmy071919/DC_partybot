[phases.setup]
providers = ["python"]
nixPkgs = [
  "...",
  "ffmpeg",
  "python3",
  "python3Packages.pip"
]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
