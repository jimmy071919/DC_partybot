[phases.setup]
nixPkgs = [
    "ffmpeg",
    "libopus",
    "python311",
    "python311Packages.pip"
]
aptPkgs = [
    "git",
    "libffi-dev",
    "libssl-dev"
]

[phases.install]
cmds = [
    "pip install --user --upgrade pip",
    "pip install --user -r requirements.txt"
]

[start]
cmd = "python main.py"
