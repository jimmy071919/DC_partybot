[phases.setup]
providers = ["python"]
nixPkgs = [
    "python310",
    "ffmpeg"
]

aptPkgs = [
    "opus-tools"
]

[phases.install]
cmds = [
    "python -m pip install --upgrade pip setuptools wheel",
    "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
