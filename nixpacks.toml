[phases.setup]
providers = ["python"]
aptPkgs = [
  "...",
  "ffmpeg",
  "opus-tools"
]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
