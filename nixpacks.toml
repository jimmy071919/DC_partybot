[phases.setup]
providers = ["python"]
nixPkgs = [
    "python3",
    "python3-pip",
    "gcc",
    "ffmpeg",
    "opus-tools"
]

[phases.install]
cmds = [
    "python3 -m pip install --upgrade pip",
    "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python3 main.py"
