app = "dc-partybot"
primary_region = "hkg"  # 選擇靠近您的區域，例如香港

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
